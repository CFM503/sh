# 更新日志

所有版本的更新内容都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [1.5.0] - 2026-09-16

### 新增功能

#### 主菜单与子菜单全自动更新
- **主菜单自动更新与热重启 (`auto_update_self`)**：
  - 启动主菜单时轻量检测远程 GitHub master 分支最新版本；
  - 发现新版本自动安全下载至临时文件，校验脚本完整性后原子覆盖本地 `menu.sh`；
  - 使用 `exec bash` 进程级热重启，无缝加载最新版主菜单。
- **子菜单增量自动更新 (`auto_update_submenu`)**：
  - 在选择执行子菜单（`network_optimize.sh`、`nginx_setup.sh`）时，自动比对远端与本地版本；
  - 自动增量静默更新至最新版并即时拉起执行。
- **高兼容纯 Bash 版本比对算法 (`version_gt`)**：
  - 基于三段式语义化语义版本比较（兼容 `v` 前缀及两位/三位版本），无需依赖外部 `sort -V` 或系统工具。
- **HTTP Range 优先加速与低延迟超时容错**：
  - 远程版本查询优先使用 HTTP Range 获取文件头部元数据，极大降低流量与耗时；
  - 默认设置 3~6 秒严格超时，网络不通或受阻时自动优雅降级跳过，不阻塞本地菜单使用。
- **依赖与更新管理升级**：
  - 主菜单新增选项「4. 检查并更新全部脚本」；
  - 「依赖管理」扩充为「依赖与更新管理」，支持可视化列表一览所有组件本地与远端版本、在线状态及一键全量/单项更新。

#### 网络优化子模块：2026 现代极限网络调优与物理硬件 FQ 持久化
- **彻底攻克 BBR 物理网卡调度器“假生效”陷阱**：
  - 动态精准嗅探当前物理默认网卡（支持 `eth0`、`ens3`、`ens5`、`enp1s0` 等各种 KVM/云网卡命名）；
  - 立即通过 `tc qdisc replace dev <iface> root fq` 激活物理硬件网卡 FQ 队列，使 BBR Pacing（精准节奏发包）完全发力；
  - 自动创建并激活 systemd 开机持久化服务 `/etc/systemd/system/set-fq-qdisc.service`，保证重启后队列永不回退。
- **内存自适应 64MB 巨型 BDP 读写缓冲区**：
  - 突破跨国千兆长距离高延迟（LFN）链路单流跑不满瓶颈；
  - 针对 ≥ 2GB 内存 VPS 自动开启【64MB 极限千兆吞吐档】；针对 < 2GB 内存 VPS 智能适配【16MB 稳健安全档】，兼顾极限性能与杜绝 OOM 风险。
- **现代化低延迟与握手加速协议栈 (0-RTT & Anti-Bufferbloat)**：
  - `net.ipv4.tcp_fastopen = 3`：开启 TCP Fast Open 双向 0-RTT 极速建连；
  - `net.ipv4.tcp_slow_start_after_idle = 0`：连接空闲时不重置拥塞窗口，彻底消除长连接与间歇网页访问再次卡顿；
  - `net.ipv4.tcp_notsent_lowat = 16384`：限制套接字未发送缓冲区为 16KB，彻底消除缓冲膨胀（Bufferbloat）与 RTT 抖动；
  - `net.ipv4.tcp_mtu_probing = 1`：自动探测路径 MTU 黑洞，解决跨境链路丢包死锁；
  - `net.core.somaxconn = 65535` & `net.core.netdev_max_backlog = 131072`：大幅扩充突发并发连接等待队列。
- **全链路真实硬件队列看板与安全回滚**：
  - 实时执行 `tc qdisc show dev <iface>` 彩色汇报物理网卡真实工作状态；
  - 备份还原时同步卸载开机持久化服务并将网卡队列还原为系统默认 `pfifo_fast`。

#### 网络优化子模块：SSH 端口安全修改 (防暴力破解)
- **网络优化菜单新增「5. 修改 SSH 端口」**：
  - 支持自定义 1024-65535 任意非占用高位端口，隐藏系统默认 22 端口，有效抵御全网自动化扫描与暴力破解；
  - **端口冲突与合法性校验**：自动排查当前端口占用状态及系统特权端口；
  - **全自动多防火墙放行**：自动检测并放行 **UFW**、**Firewalld** 以及 **iptables** 端口策略，杜绝因防火墙未开导致失联；
  - **SELinux 策略自适应**：在 CentOS/RHEL/Fedora 系统下自动为新端口配置 `ssh_port_t` 标签；
  - **语法检测与瞬时回滚保障**：修改后通过 `sshd -t` 进行严格语法检测，若异常自动回滚配置且不重启服务；
  - **安全引导与测试建议**：终端高亮提示云平台安全组放行指引与独立新会话测试命令。

---

## [1.4.2] - 2026-08-31

### 优化与编码/死链自愈

#### 历史乱码文件自动清理与重复项消除
- **环境初始化自愈 (`clean_corrupted_configs`)**：在脚本初始化（`init_env`）时自动扫描 `/etc/nginx/sites-available/`、`sites-enabled/` 及 `conf.d/` 目录：
  1. 自动检测并删除死链接（指向已不存在目标文件的软链接）；
  2. 自动检测并清除包含非标准 ASCII 乱码字符的历史废弃文件名（如旧版生成的 `proxy_pyway.conf`）；
  3. 自动检测并清除包含损坏二进制字节的非文本配置文件，彻底消除 `grep binary file matches` 及重复列出问题。

---

## [1.4.1] - 2026-08-31

### 优化与容错增强

#### 连通性检测与服务守护
- **重载智能自愈**：在执行 `reload_nginx` 重载配置时，自动检测 Nginx 运行状态，若服务未启动则自动平滑启动，彻底避免因服务未启动导致的重载与探测失败。
- **扫描容错过滤**：全量检测（菜单 6）与查看配置（菜单 3）仅扫描已启用的有效文本配置文件，并增加 `grep -I` 二进制/损坏文件自动过滤，彻底解决损坏旧配置引发的 `binary file matches` 报错。
- **删除同步清理**：删除独立代理时同步清理 `sites-available`、`sites-enabled` 及 `conf.d` 中的所有关联文件。

---

## [1.4.0] - 2026-08-31

### 里程碑版本发布

#### 1. 反向代理智能检测与即时反馈系统
- **全自动生效检测报告**：添加反向代理（菜单 4 / 菜单 5）完成后，立即发起全链路探测，直观打印配置语法、Nginx 端口、后端进程（如 goway PID）及真实 HTTP 状态码（200/400/502 等）。
- **新增菜单 6. 测试代理连通性**：一键全量扫描并探测服务器上的所有独立代理站点与默认站点路径代理。

#### 2. 安全反向代理管理与出厂恢复
- **安全路径注入**：彻底解决 location 处于 http 上下文顶层的问题，智能注入至默认站点 `server` 块内，并支持自动回滚机制。
- **独立站点安全隔离**：独立站点统一存放于 `/etc/nginx/conf.d/` 或 `sites-available/`，配置标准完整。
- **一键恢复初始配置**：支持【官方原生出厂重置】与【本地快速修复重置】。

#### 3. Nginx 深度彻底卸载 (Purge)
- 全量清理主程序、所有依赖模块（`nginx-*`, `libnginx-mod-*`）、配置文件（`/etc/nginx`）、缓存日志及 systemd 服务单元残留，并在卸载前自动归档备份。

#### 4. 交互稳定性提升
- 修复所有菜单循环中的 EOF 处理，保障管道与自动化脚本稳定执行。

---

## [1.3.3] - 2026-08-31

### 新增与优化

#### 反向代理成功状态即时反馈与连通性检测
- **即时生效检测报告**：在菜单【4. 添加反向代理】及【5. 快捷添加预设代理】配置完成后，自动执行即时生效检测，全方位输出：
  1. 配置语法测试结果
  2. Nginx 服务端口监听状态
  3. 后端服务目标进程与监听状态（自动识别如 goway、PID 等）
  4. HTTP 真实探测状态码（200/400/404/502 等）
  5. 外部公网访问测试 URL
  6. 直观的【成功生效】/【后端未就绪】状态横幅
- **新增菜单 6. 测试代理连通性**：Nginx 管理菜单新增独立选项，支持一键全量扫描当前所有独立站点与默认站点路径反代，实时检测 HTTP 连通状态。

---

## [1.3.2] - 2026-08-31

### 优化与稳定性增强

#### 菜单交互与自动化支持
- **EOF 安全处理**：全面优化所有脚本（`menu.sh`、`nginx_setup.sh`、`network_optimize.sh`）中的 `read` 菜单循环，增加 `read || break` 安全退出机制，避免在管道或自动化脚本中发生死循环。
- **真机全面测试验证**：在 Debian VPS 真机环境完成所有 15+ 项子菜单与功能（BBR、反代、恢复默认、删除配置、重载/重启）的端到端自动化测试与验证。

---

## [1.3.1] - 2026-08-31

### 优化与增强

#### Nginx 管理与配置 (nginx_setup.sh)
- **深度彻底卸载 (Purge)**：重构并增强卸载功能，支持全量清除 `nginx`、`nginx-common`、`nginx-core`、`nginx-full` 以及 `libnginx-mod-*` 等动态扩展模块与孤立依赖包。
- **全方位残余文件清理**：卸载时自动清理 `/etc/nginx/`、`/var/log/nginx/`、`/var/cache/nginx/`、`/usr/share/nginx/`、`/run/nginx.pid` 等所有配置文件、日志与缓存。
- **自动安全备份**：在完全卸载（Purge）前，自动对历史配置进行带时间戳的完整归档备份（保存在 `/root/nginx_backup_before_purge_*`），防止误操作造成配置丢失。
- **服务与进程管理**：卸载时自动停止并强制清理可能残留的 Nginx 僵尸进程，自动执行 `systemctl daemon-reload` 和 `reset-failed` 注销服务并彻底释放 80/443 端口。
- **零系统影响保障**：确保卸载过程仅作用于 Nginx 体系，绝对不影响 SSH（22 端口）、后端服务（如 goway 2052 端口）、网络内核（BBR/Sysctl）以及用户已有网站数据。

---

## [1.3.0] - 2026-08-31

### 重构与修复

#### Nginx 管理与配置 (nginx_setup.sh)
- **解决默认配置损坏问题**：改用模块化独立配置文件（`/etc/nginx/conf.d/proxy_*.conf`）与安全注入机制，彻底废除脆弱的 sed 暴力修改。
- **出厂初始配置还原**：恢复初始配置新增【官方出厂原生重置】与【本地快速重置】，支持通过包管理器 100% 还原刚安装时的全新配置文件。
- **修复卸载 Bug**：修复了原脚本在卸载 Nginx 时误执行安装命令的严重逻辑错误。
- **交互式删除代理**：支持动态扫描全部独立代理与注入标记，以编号列表交互式选择安全删除。
- **WebSocket 映射增强**：新增全局 `00_websocket_map.conf` 映射，完美兼顾 HTTP 与 WebSocket 长连接。
- **自动回滚机制**：添加/修改代理时若 `nginx -t` 测试失败，自动瞬间回滚至历史备份，保障服务高可用。

---

## [1.1.0] - 2026-01-31

### 新增

#### 自动下载功能
- menu.sh 现在可以自动下载子菜单功能
- 启动时自动检查缺失的依赖文件
- 支持手动更新/检查依赖
- 菜单新增"更新/检查依赖"选项

#### 依赖管理
- 一键检查所有依赖文件状态
- 一键更新所有依赖文件
- 一键删除所有依赖文件
- 支持单独更新每个依赖文件

### 优化
- 只需下载 menu.sh 即可使用所有功能
- 自动从 GitHub 下载缺失的子菜单文件
- 更友好的错误提示

---

## [1.0.0] - 2026-01-31

### 新增

#### 主菜单 (menu.sh)
- 创建通用菜单框架，兼容所有Linux发行版
- 系统管理模块：查看系统信息、CPU、内存、磁盘
- 网络管理模块：IP、连接、路由、DNS、端口
- 文件管理模块：浏览、查找、复制、删除
- 服务管理模块：启动、停止、重启服务
- 用户管理模块：添加、删除用户和用户组
- 软件管理模块：自动检测包管理器
- 备份恢复模块：文件和目录备份
- 系统监控模块：进程、CPU、内存、网络监控
- 安全管理模块：防火墙、端口、日志

#### 网络优化 (network_optimize.sh)
- 自动检测系统版本(Debian 12/13、Ubuntu、CentOS等)
- 自动选择配置文件路径
- BBR拥塞控制配置
- TCP缓冲区优化(16MB)
- 滑动窗口和SACK优化
- 自动备份原有配置
- 配置验证功能
- 恢复备份功能

#### Nginx配置 (nginx_setup.sh)
- 自动安装Nginx
- 支持Debian/Ubuntu/CentOS/RHEL
- 自动配置反向代理
- WebSocket支持
- 预设: /pyway -> 127.0.0.1:2052
- 自定义代理配置
- 配置语法检查
- 自动备份和恢复

### 特性
- 支持所有主流Linux发行版
- 自动检测包管理器
- 颜色输出，界面友好
- 安全确认机制
- 自动备份功能